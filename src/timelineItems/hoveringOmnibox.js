import React from 'react';
import {View} from 'react-native';

const Omnibox = (props) => 
    (<View style={{justifyContent: "flex-start", flexGrow: 1, width: "100%", height: 70, bottom: 0, position: "absolute", backgroundColor: "transparent"}}>
        <View style={{height: 50, backgroundColor: "orange"}}>
            </View>
            </View>);


export default Omnibox;