import React from 'react';
import {View} from 'react-native';
import uuid from 'react-native-uuid';

class FutureFeedItem extends React.PureComponent {
    render() {
       return  <View style={{width: 200, height: 50, backgroundColor: "brown", margin: 10}} />
    }
}

export default (FutureFeedItem);